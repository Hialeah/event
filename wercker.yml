box: java

dev:
  steps:
    - script:
      name: gradle bootRun
      code: |
        ./gradlew bootRun

# Build Application
build:
  steps:
  
    - script:
        name: Display Message
        code: |
            echo "This works fine"
